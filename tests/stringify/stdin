#define str(x)	# x

str(this is a series of preprocessing tokens)
str(same   but	with/* a lot of */whitespace)
str(this "macro" contains a "string")
str(beware that "strings\tshould\nbe   \"escaped\"" specially)

/*
 * Taken from the GCC docs
 */
str(p = "foo\n";)
